spring:
  application:
    name: AutoCareerBridge
  datasource:
    url: jdbc:mysql://localhost:3306/autocareerbridge?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379
  mail:
    username: quocta.gov@gmail.com
    password: nrkllzwwziuemlpi
    host: smtp.gmail.com
    port: 587
    protocol: smtp
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
          ssl:
            trust: smtp.gmail.com

media:
  img_path: ${user.home}${file.separator}Downloads${file.separator}images

jwt:
  signerKey: 4vWibUUTG0xqSCoRSLJD/pSWelkjvZuMFnrp2cRGlSuqCDCsZ+ovnMbFcem13+UQ

security:
  default:
    username: admin
    password: admin

server:
  port: 1111
