# Application name
spring.application.name=AutoCareerBridge

# Datasource configuration
spring.datasource.url=jdbc:mysql://192.168.0.152:3306/auto_career_bridge_java_2?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=tts-java2
spring.datasource.password=Java@tts2
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# HikariCP connection pool configuration
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.pool-name=HikariPool-AutoCareerBridge

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Redis configuration
spring.data.redis.host=192.168.0.152
spring.data.redis.port=6379

# Mail configuration
spring.mail.username=quocta.gov@gmail.com
spring.mail.password=nrkllzwwziuemlpi
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Media configuration
media.img_path=${user.home}${file.separator}Downloads${file.separator}images

# JWT configuration
jwt.signerKey=4vWibUUTG0xqSCoRSLJD/pSWelkjvZuMFnrp2cRGlSuqCDCsZ+ovnMbFcem13+UQ

# Server configuration
server.port=8081
server.servlet.context-path=/auto-career

# Logging configuration
logging.level.org.springframework.data.redis.core=DEBUG
logging.level.org.springframework.data.redis.connection=DEBUG

# Hibernate logging (optional for debugging connection issues)
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql=OFF

